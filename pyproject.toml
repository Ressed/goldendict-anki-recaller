[project]
name = "goldendict-anki-recaller"
version = "0.1.0"
description = "Recall existing Anki cards from GoldenDict-ng into today's study queue."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "jinja2==3.1.6",
    "markupsafe==3.0.3",
    "simplemma==2.0.0",
]

[tool.uv]
package = false
